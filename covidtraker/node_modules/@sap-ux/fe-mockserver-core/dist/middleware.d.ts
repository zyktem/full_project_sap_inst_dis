import type { MockserverConfiguration } from './api';
import type { IRouter } from 'router';
import type { IFileLoader, IMetadataProcessor } from './index';
/**
 * Creates and configure the different middleware for each mocked service.
 *
 * @param newConfig
 * @param app
 * @param fileLoader
 * @param metadataProcessor
 */
export declare function createMockMiddleware(newConfig: MockserverConfiguration, app: IRouter, fileLoader: IFileLoader, metadataProcessor: IMetadataProcessor): Promise<void>;
//# sourceMappingURL=middleware.d.ts.map